# CoreMain — 專案中心思想（North Star）

> 本檔由 PM 在專案最開始、與使用者確認後建立，是整個專案的「中心思想」。
> 之後所有功能開發、測試、驗收都以本檔為中心：圍繞主題、維持簡單順手不複雜。
> 最先建立、最少變動；主題若要變更，需經使用者確認並回頭檢查既有功能是否仍對齊。

專案代號：**contorell**（Control + Roll，權限盤點與比對）
建立日期：2026-08-04

---

## 一句話專案主題（Theme）

**以 AD 使用者為權限基準，讓管理者能把「AD 群組」與「其他系統帳號／其他 AD 群組」拉在一起比對差異，找出該給沒給、該收沒收的權限，並在自訂欄位中留下說明。**

## 核心價值（North Star）

- 使用者用它最想得到的那一件事：
  **一眼看出「兩份名單哪裡不一樣」，而且知道每一筆差異為什麼存在。**
- 成功長什麼樣（一句話）：
  **管理者選定兩個來源、按下比對，30 秒內拿到一份可逐筆標註說明並匯出的差異清單。**

## 範圍主軸（圍繞主題）

### 圍繞主題會做 ✅

1. **AD 為權威來源**：以 LDAP/LDAPS 讀取 AD 使用者與群組（含巢狀群組展開）。
2. **多來源接入**：同一套「帳號清單」抽象，支援三種取得方式
   - LDAP 抓取（AD 群組成員）
   - API 抓取（可設定的通用連接器：URL／驗證／JSON 欄位對應）
   - Excel／CSV 匯入（手動上傳名單）
3. **比對引擎**：任兩個來源做集合比對，輸出四種狀態
   - 僅存在於 A（B 缺漏）
   - 僅存在於 B（A 未授權／孤兒帳號）
   - 兩邊都有（相符）
   - 兩邊都有但屬性不一致（如 AD 已停用但外部系統仍啟用）
4. **稽核情境**：離職／異動人員稽核（AD 停用或已刪除，外部系統仍存活）。
5. **快照與變更追蹤**：每次抓取存成快照，可比對「這次 vs 上次」，追蹤權限何時被加／被移除。
6. **自訂欄位描述**：管理者可對每一筆比對結果填寫自訂欄位（如處理狀態、負責人、風險說明），並隨比對結果保存與匯出。
7. **匯出報表**：差異清單可匯出 Excel／CSV 供稽核留存。

### 與主題無關、明確不做 ❌（防 scope creep）

- ❌ **不做權限異動寫回**：本系統**唯讀**，只比對與記錄，不代替管理者去 AD 或外部系統建立／刪除／停用帳號。（降低風險，也大幅簡化安全模型）
- ❌ 不做完整的 IAM／IGA 平台（不做簽核工作流、角色工程、SoD 規則引擎）。
- ❌ 不做 AD 本身的管理介面（不取代 ADUC）。
- ❌ 不做 SSO／身分提供者（本系統是消費 AD，不是提供身分）。
- ❌ 不做即時同步／事件訂閱（採排程＋手動觸發的快照模式即可）。

## 設計原則（所有功能一律遵守）

- **簡單**：用最少的概念達成目的。整個系統只有四個核心概念：**資料來源（Source）→ 快照（Snapshot）→ 比對（Comparison）→ 標註（Annotation）**。不再增加第五個概念。
- **順手**：管理者的主要動作路徑最短——「選來源 A、選來源 B、按比對、看結果、填說明、匯出」六步完成，不需要看說明書。
- **不複雜**：三種資料來源（LDAP／API／Excel）共用**同一個介面**，比對引擎完全不知道資料從哪來；新增來源型態不必改比對邏輯。
- **取捨準則**：「功能更強」與「更簡單順手」衝突時，預設選簡單順手，除非主題明確要求。
  - 例：不做可視化拖拉的規則設計器，只做「選 A、選 B、比對」——因為後者已能解決 90% 稽核需求。

## 主題對照檢核（每加一個功能前先自問）

- [ ] 這個功能是在服務上面的「專案主題」嗎？（否 → 不做，或退回 PM 對齊）
- [ ] 有沒有更簡單、更順手的做法達成同樣目的？
- [ ] 它會不會讓整體變複雜、偏離主軸？
- [ ] 它有沒有讓系統從「唯讀比對工具」變成「權限異動工具」？（是 → 明確不做）


---

## 【Round 8 修訂】主畫面改為「人員中心權限檢視」（經使用者確認）

> 使用者確認：核心互動從「任兩來源比對」改為**以 AD 已啟用人員為主檔**，
> 逐一檢視每人在各權限群組（網路權限／VPN／LINE…）是否具有權限，並標出
> 「帳號已停用卻仍在群組」的異常。仍在原主題「AD 為基準的唯讀權限稽核」內，
> **維持唯讀**（不寫回）。原「兩來源比對」保留為次要功能，不移除。
> 核心概念維持不新增第五個：主檔＝權威來源快照、權限欄＝其餘來源快照、
> 異常＝集合差 + 停用旗標，全部建構在既有 來源→快照→比對→標註 之上。
